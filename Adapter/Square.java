public class Square {
    void draw(int x, int y, int width, int height) {
        System.out.println("Drawing square from cornern botttom-left: " +
        x + " " + y + " with dimensions (w,h): " + width + " " + height);
    }
}
